var ball, paddle, rect1, rect2, bg, oppPaddle1, oppPaddle2;

function preload(){
  bg = loadImage("/bg.png");
  ball = loadImage("/bball.png");
}

function setup(){
  var paddle = createSprite();
}

function draw(){
  createCanvas(800, 400);
}